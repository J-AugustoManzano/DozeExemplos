/* ALG09 */
fun main() {
  var X: Int
  var Y: Int
  X = readLine()!!.toInt()
  if (X >= 10)
    Y = Math.pow(X.toDouble(), 2.0).toInt()
  else
    Y = Math.pow(X.toDouble(), 2.0).toInt()
  println(Y)
}
