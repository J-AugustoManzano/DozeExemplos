// ALG06
import Foundation
func main() {
    var X: Int = 0
    if let input = readLine(), let intValue = Int(input) {
        X = intValue
    }
    if (X > 100) {
        print(X)
    }
}
main()
