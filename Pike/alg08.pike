// ALG08 
int main()
{
  int X = 0;
  int Y = 0;
  sscanf(Stdio.stdin.gets(), "%d", X);
  sscanf(Stdio.stdin.gets(), "%d", Y);
  if (X <= Y) {
    write("%d\n", X);
  } else {
    write("%d\n", Y);
  }
  return 0;
}
