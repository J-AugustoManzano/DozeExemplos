// ALG05 
int main()
{
  int X = 0;
  int Y = 0;
  int Z = 0;
  sscanf(Stdio.stdin.gets(), "%d", X);
  sscanf(Stdio.stdin.gets(), "%d", Y);
  Z = pow(X, 2) + pow(Y, 2);
  write("%d\n", Z);
  return 0;
}
