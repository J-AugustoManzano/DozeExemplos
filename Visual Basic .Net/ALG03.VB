' ALG03
Module MainModule
    Sub Main()
        Dim X As Integer
        Dim Y As Integer
        Integer.TryParse(Console.ReadLine(), X)
        Y = Math.Pow(X, 2)
        Console.WriteLine(Y)
    End Sub
End Module
