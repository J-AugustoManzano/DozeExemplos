' ALG06
Module MainModule
    Sub Main()
        Dim X As Integer
        Integer.TryParse(Console.ReadLine(), X)
        If (X > 100) Then
            Console.WriteLine(X)
        End If
    End Sub
End Module
